apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: distroai
  labels:
    app: web
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: web
